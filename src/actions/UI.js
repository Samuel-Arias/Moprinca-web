import { types } from "../types/types";

export const hiddenLogo = () => (
    {
        type: types.hiddenLogo
    }
)

export const showLogo = () => (
    {
        type: types.showLogo
    }
)

export const openNavbar = () => (
    {
        type: types.openNavbar
    }
)

export const closeNavbar = () => (
    {
        type: types.closeNavbar
    }
)
